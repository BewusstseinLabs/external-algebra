
use linear_algebra::vector::Vector;

use crate::operators::{
    InteriorProduct,
    ExteriorProduct
};

/*
impl<T, const COL: usize> InteriorProduct for Vector<T, COL>
where
    T: Default + Debug + Copy
{
    type Output;

    fn interior_product( self, rhs: Self ) -> Self::Output {

    }
}

impl<T, const COL: usize> ExteriorProduct for Vector<T, COL>
where
    T: Default + Debug + Copy
{
    type Output;

    fn exterior_product( self, rhs: Self ) -> Self::Output {

    }
}
*/